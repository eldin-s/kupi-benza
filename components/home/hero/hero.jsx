import { View, Text } from "react-native";
import SearchCard from "./search-card";
import Slider from "./slider";

const Hero = () => {
  return (
    <View>
      <SearchCard />
      <Slider />
    </View>
  );
};

export default Hero;
